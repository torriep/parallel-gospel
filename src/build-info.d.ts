// Injected at build time by vite.config.ts (define). Shown in SettingsPanel
// so a TestFlight build can be matched to the exact commit it contains.
declare const __BUILD_COMMIT__: string;
declare const __BUILD_DATE__: string;
